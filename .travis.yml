language: python
python:

  - "nightly"


# command to install dependencies
install:
  - pip install -r requirements.txt
  

# command to run tests
script: python testPython.py

# Testing multiple operating systems
os:
  - linux
  - osx
